<div>
    <h3>Preview</h3>

    <iframe
        src="{{ path('admin_document_open_file', { id: entity.instance.id }) }}"
        title="Document preview"
        width="100%"
        height="800"
    ></iframe>
</div>
