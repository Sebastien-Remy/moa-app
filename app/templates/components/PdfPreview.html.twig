<div class="border rounded overflow-hidden">
    <iframe
        src="{{ src }}"
        title="{{ title|default('Document preview') }}"
        width="100%"
        height="{{ height|default(900) }}"
        style="display: block; border: 0;"
    ></iframe>
</div>
